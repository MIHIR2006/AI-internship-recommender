# Production Environment Configuration for Render
# Copy this file and set values in Render dashboard

# Database URL - Will be automatically set by Render
# Format: postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME
# Render will provide this via DATABASE_URL environment variable

# API Keys - Set these in Render dashboard
GOOGLE_API_KEY=your_google_api_key_here
HUGGINGFACE_API_TOKEN=your_huggingface_token_here

# Security - These will be auto-generated by Render
SESSION_SECRET=auto_generated_by_render
JWT_SECRET_KEY=auto_generated_by_render

# Database settings
RUN_DB_CREATE_ALL=true
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# CORS origins - Update with your frontend URL
CORS_ORIGINS=https://your-frontend-app.onrender.com,http://localhost:3000,http://localhost:5173

# Vector store directories (mounted volumes in production)
VECTORSTORE_DIR=/var/data/vectorstores
STUDENT_VECTORSTORE_DIR=/var/data/student_vectorstores

# Production optimizations
PYTHONUNBUFFERED=1
